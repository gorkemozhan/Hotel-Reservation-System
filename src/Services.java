
public abstract class Services implements Calculable {
	
	protected abstract String getServiceType();
	
	
	protected static double calculateService() {
		//double total = Spa.calculateService()+Laundry.calculateService() ;
		//return total;
		return 0;
	};
}
